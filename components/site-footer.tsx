export function SiteFooter() {
  return (
    <footer className="mt-8 border-t border-border">
      <div className="mx-auto w-full max-w-6xl px-4 py-8 text-center font-mono text-[11px] tracking-[0.06em] text-muted-foreground uppercase">
        Dates can change. Always confirm on the official scholarship page.
      </div>
    </footer>
  );
}
